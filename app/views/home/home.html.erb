<!--
<h1>Home</h1>


<% if logged_in? %>
<label>Your clubs are currently reading:</label>
<ul>
<% @user.clubs.each do |club| %>

    <li><%= link_to club.name, club_path(club)%> - <%= club.book.title%></li>

<%end%>
</ul>

<%= link_to "Create New Book Club", new_club_path%> •
<%= link_to "Browse Book Clubs", clubs_path%>
<%else%>

<label>All clubs:</label>
<ul>
<% @clubs.each do |club| %>
<li><%= link_to club.name, club_path(club)%> </li>
<%end%>
</ul>
<%end%>
-->

<% if logged_in? %>

    <h2>Your clubs are currently reading:</h2>
        <ul>
            <% @user.clubs.each do |club| %>
                <li><%= link_to club.name, club_path(club)%> - <%= club.book.title%></li>
            <%end%>
        </ul>

    <%= link_to "Create New Book Club", new_club_path%> • <%= link_to "Browse Book Clubs", clubs_path%>
<%else%>
<!-- Slide Show -->

<section>
  <% @books.all_img_urls.each do |img_url|%>
  <img class="mySlides" src=<%= img_url%> style="width:200px">
  <%end%>
</section>

<script>
// Automatic Slideshow - change image every 3 seconds
var myIndex = 0;
carousel();

function carousel() {
  var i;
  var x = document.getElementsByClassName("mySlides");
  for (i = 0; i < x.length; i++) {
     x[i].style.display = "none";
  }
  myIndex++;
  if (myIndex > x.length) {myIndex = 1}
  x[myIndex-1].style.display = "block";
  setTimeout(carousel, 3000);
}
</script>

<%end%>