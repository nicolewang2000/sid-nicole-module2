<%= comment.user.full_name %> : <%= comment.content %> <br> (<%= time_ago_in_words(comment.created_at)%>)
<!--
<% if comment.user_id == session[:user_id]%>
    <%= link_to 'Delete', [comment.club, comment], method: :delete, data: { confirm: "Are you sure?" }%>
<%end%>
-->
 <br>